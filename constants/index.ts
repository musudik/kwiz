/**
 * KWIZ Design System - Constants Index
 * Export all design system constants
 */

// Main theme
export { getThemedColors, default as theme, type Theme, type ThemeMode } from './Theme';

// Colors
export { default as colors, colorsDark, palette } from './Colors';

// Typography
export { fontSizes, fonts, letterSpacing, lineHeights, default as textStyles } from './Typography';

// Spacing & Layout
export { borderRadius, layout, safeArea, screen, shadows, spacing, zIndex } from './Spacing';

// Animations
export { animationValues, durations, easings, haptics, springConfigs, timingConfigs } from './Animations';

